<?xml version="1.0"?>
<psalm
    errorLevel="1"
    findUnusedBaselineEntry="true"
    findUnusedCode="false"
    ensureOverrideAttribute="true"
    strictBinaryOperands="true"
    cacheDirectory="./runtime/cache/psalm"
    allowStringToStandInForClass="false"
    disableVarParsing="true"
    memoizeMethodCallResults="true"
    hoistConstants="true"
    resolveFromConfigFile="true"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
>
    <projectFiles>
        <directory name="src" />
        <file name="public/index.php" />
        <file name="yii" />
        <ignoreFiles>
            <directory name="vendor" />
            <directory name="runtime" />
            <directory name="tests" />
        </ignoreFiles>
    </projectFiles>

    <plugins>
        <!-- Плагин для строгой типизации -->
    </plugins>

    <issueHandlers>
        <!-- Уровень ошибок задаётся в attribute errorLevel -->
    </issueHandlers>
</psalm>